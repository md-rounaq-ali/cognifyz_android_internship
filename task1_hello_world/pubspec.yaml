name: task1_hello_world
description: "Cognifyz Internship - Task 1: Hello World App (Level 1 - Beginner)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: '>=6.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: '>=3.0.0'

flutter:
  uses-material-design: true
